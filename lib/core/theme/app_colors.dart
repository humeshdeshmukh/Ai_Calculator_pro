import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors
  static const Color primaryGold = Color(0xFFD4AF37);
  static const Color primaryPurple = Color(0xFF7C3AED);
  static const Color primaryBlue = Color(0xFF007AFF);
  static const Color primaryGray = Color(0xFF8E8E93);

  // Background Colors
  static const Color darkBackground = Color(0xFF1C1C1E);
  static const Color lightBackground = Color(0xFFF2F2F7);

  // Surface Colors
  static const Color darkSurface = Color(0xFF2C2C2E);
  static const Color lightSurface = Colors.white;

  // 10 Color Themes (as mentioned in feature.md)
  static const List<Color> themeColors = [
    Color(0xFF007AFF), // Classic Blue
    Color(0xFF7C3AED), // Royal Purple
    Color(0xFFD4AF37), // Premium Gold
    Color(0xFFFF3B30), // Warning Red
    Color(0xFF34C759), // Success Green
    Color(0xFFFF9500), // Energy Orange
    Color(0xFF5856D6), // Deep Indigo
    Color(0xFFAF52DE), // Soft Purple
    Color(0xFF5AC8FA), // Sky Blue
    Color(0xFFFF2D55), // Vibrant Pink
  ];
}
